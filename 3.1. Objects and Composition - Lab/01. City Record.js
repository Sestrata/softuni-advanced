function cityRecord(name, population, treasury) {

    let resultObj = {
        name: name,
        population: population,
        treasury: treasury
    };

    //or:
    // let resultObj = {};
    // resultObj.name = name;
    // resultObj.population = population;
    // resultObj.treasury = treasury;
    
    return resultObj;
}
